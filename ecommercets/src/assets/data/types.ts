export type A = { id: number, image: string };

export type B = { id: number; title: string; desc: string };

export type C = { id: number; title: string; cover: string };

export type D = {
  id: number;
  name: string;
  price: number;
  qty: number;
  category: string;
  cover: string;
  desc: string;
};
